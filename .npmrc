# The build needs devDependencies: prisma (generate), typescript, tailwindcss
# and tsx all live there. Hosts commonly set NODE_ENV=production for the whole
# pipeline, and npm then omits dev dependencies by default, which breaks the
# build long before it reaches next build.
#
# `include=dev` forces them in regardless of NODE_ENV.
include=dev
